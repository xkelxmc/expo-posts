import React from 'react';
import { Text, View } from 'react-native';

const TextBody = ({ text }) => {
    return (
        <View>
            <Text>{text}</Text>
        </View>
    );
};

export default TextBody;
